<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidproject_maps">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_fticon"
        android:label="이츠 웨어"
        android:roundIcon="@mipmap/ic_launcher_fticon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Review_ReadActivity"></activity>
        <activity android:name=".Review_WriteActivity" />
        <activity android:name=".OrderFinishActivity" />
        <activity android:name=".MyinfoActivity" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".LogindetailActivity" />
        <activity android:name=".MypageActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".ShopDetailActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".Order"
            android:parentActivityName=".ShopInfoActivity" />
        <activity
            android:name=".ShopInfoActivity"
            android:label="Eat Whereable"
            android:parentActivityName=".MapsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCjyBnLQkhRp8BHplAdR1RCh64KcnuVva4" />

        <activity
            android:name=".MapsActivity"
            android:label="Eats Where" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>